#!/bin/sh

set -eu

SCRIPT_DIR=$(CDPATH='' cd -P -- "$(dirname -- "$0")" && pwd -P)

if ! command -v python3 >/dev/null 2>&1; then
    echo "native-original-runner: python3 is required" >&2
    exit 127
fi

exec python3 -S "$SCRIPT_DIR/runner.py" "$@"
