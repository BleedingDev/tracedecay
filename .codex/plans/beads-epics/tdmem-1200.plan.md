---
name: tdmem-1200
overview: "Keep the product derivative close enough to future TraceDecay V2 changes that coding agents can integrate Zackary Jackson's work repeatedly without turning every sync into a manual rewrite."
todos:
  - id: tdmem-1200-deliver
    content: "Deliver Bead tdmem-1200: Continuous \u2014 Preserve convergence with Zack and ingest external lessons safely; satisfy every recorded acceptance criterion, run the smallest behavioral dependency cone, then commit and push the green slice."
    status: pending
isProject: false
---

# tdmem-1200: Continuous — Preserve convergence with Zack and ingest external lessons safely

## Execution Notes

Beads issue: `tdmem-1200`. Current Beads status at generation: `open`.

Keep the product derivative close enough to future TraceDecay V2 changes that coding agents can integrate
Zackary Jackson's work repeatedly without turning every sync into a manual rewrite.

Design authority:

Track an exact upstream floor, classify incoming changes, isolate product patches, record semantic invariants,
and run parity plus product regression gates on every sync train. Jeffrey-derived ideas enter through explicit
feature/invariant intake rather than wholesale coupling to his runtime.

Acceptance authority:

- [ ] Every product patch has an owner, upstream touch list, and semantic invariant.
- [ ] A documented sync train can advance from one Zack checkpoint to the next.
- [ ] Generated contracts, Native parity, provider conformance, and product journeys gate convergence.
- [ ] External lessons are linked to source commits and reimplemented behind product-owned boundaries.

## Constraints

- Beads is the live source of truth. Re-read `br show tdmem-1200` and require `br ready` before a new claim.
- Unsatisfied blocking Beads dependencies at generation: none.
- Beads parent/hierarchy references: tdmem-0000. Parent readiness is enforced by `br`, not fabricated as a plan dependency.
- Build semantic producers/consumers and focused evidence before bookkeeping. Do not create hash-, receipt-, lock-, or presence-only gates.
- Root alone owns heavy Cargo execution, integration acceptance, commits, pushes, and Beads closure.
- Workers must stay inside their assigned write scope, preserve concurrent edits, and stop rather than widen scope.
- Do not contact upstream maintainers or mutate external work items.

## Operator Guidance

Intersect `plan-graph frontier` with live `br ready`; launch only Beads-ready or already-claimed nodes. Assign one coherent file/module owner, run focused checks, review the exact diff locally, then publish the green slice immediately. Close the Bead only after every acceptance item and real journey required by the issue are evidenced.
