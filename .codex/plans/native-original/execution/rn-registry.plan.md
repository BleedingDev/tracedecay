---
name: rn-registry
overview: "Register Native and NCM with truthful independent capabilities. Preserve the complete original upstream Native implementation from 570 and its operation boundaries."
todos:
  - id: rn-registry-done
    content: "Register Native and NCM with truthful independent capabilities and return the required reviewable evidence."
    status: pending
isProject: false
---

# Register Native and NCM with truthful independent capabilities

## Execution Notes

Read ../WORKER-RULES.md and ../REVIEWED-DECISIONS.md before this node. The reviewed decisions override provisional recommendations in audit reports. Worktree: /Users/satan/workspace/bleedingdev/projects/tracedecay/.worktrees/pluggable-memory-providers-v2. Use this exact workdir on every shell call; confirm the actual execution HEAD and peer changes before editing.

Execution host: Codex. Use an available Codex-native execution/review agent when assigned; adapt unavailable model preferences within Codex. You are a leaf; no child agents or cross-host agent CLI launches. Preserve peers' edits and send cross-scope needs to the lead.

Mode: write-capable.
Prerequisites: rn-native-port. Every named predecessor must be accepted by root before dependent work starts. Original baseline: 57006f60cb45bcee8487e73a40d4fad1a12ee2b6; audited product head: 1fe250fed7ca615f1dfdcd580befeb276328e910.

Read these reports under ../evidence/: core-interface.md, native-adapter.md, host-delivery.md, ncm-boundary.md. Also read the exact predecessor outputs supplied by root; do not infer an unfinished API.

## Ownership and Constraints

Write only:

- crates/tracedecay-memory-provider-registry/** except Cargo manifests

Everything else is out of scope. Original Native/session/LCM implementations, original storage and contracts, NCM internals, live databases/settings, unrelated docs, manifests and generated files are protected unless explicitly named above. An allowed directory does not permit editing an original file protected by the reviewed decision. Shared generated outputs outside this ownership require an explicit root assignment before generation writes them.

No push, merge, release, global install, runtime user-data action or unassigned cleanup. Only the designated build owner in rn-build-reference and rn-build submits Cargo work. A no-change conclusion is valid when evidence proves the required behavior already holds; it must not hide missing coverage.

## Steps

1. Consume accepted Native wrapper exports and the contract decision. Remove staged-specific reexports, scope bindings and capability assumptions.
2. Preserve the existing per-registration common-profile bit and recorded scope bindings. Native can mount without common advisory completion; NCM/injected active providers retain the required common validation.
3. Preserve pinned selection and history authority. Keep the original Native context route identifiable without issuing a duplicate Native provider call or fabricating an empty success.
4. Change context pack code only if the accepted route identity requires it; keep generic budgets/provenance policy and NCM output behavior unchanged.
5. Update focused registration/call-plan tests and publish exact mount contract to composition.

The canonical Native delivery marker is host-owned registration metadata pinned to selected provider/revision and the actual canonical request. It cannot be inferred from a display name or accepted from provider-controlled claims. The host owner must receive an explicit contract that permits skipping the additional Native invocation, not merely deduplicating its output.

## Acceptance Checklist

- Native mounts with truthful capabilities; NCM still rejects an incomplete common profile.
- Disabled/observer/active identities remain distinct and no provider-name inference appears.
- Native context route and NCM advisory route are represented without duplicate calls or fabricated terminal evidence.

## Operator Guidance

Root launches this node from the saved execution graph only when its predecessors are accepted and its exact files are free. Review this diff before releasing dependent writers. Root alone updates plan status.

Return: node ID; exact changed paths and reasons; diff; verification commands and actual results; build tickets if applicable; protected-behavior evidence; unresolved dependencies/failures with exact next owner. Do not claim an unrun check passed.

Stop condition: Return registry diff and mount inputs. Do not edit host context, Native bridge or NCM internals.

## Current dependency contract

Prerequisites: rn-native-port. The exact edges in ../execution-selection.json are authoritative; this section supersedes older prerequisite prose. Read ../READINESS-PLAN.md for the latest NCM repair and semantic scope.
