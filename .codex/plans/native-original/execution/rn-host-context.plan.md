---
name: rn-host-context
overview: "Deliver original Native context once through its original host path. Preserve the complete original b3 Native implementation and its operation boundaries."
todos:
  - id: rn-host-context-done
    content: "Deliver original Native context once through its original host path and return the required reviewable evidence."
    status: pending
isProject: false
---

# Deliver original Native context once through its original host path

## Execution Notes

Read ../WORKER-RULES.md and ../REVIEWED-DECISIONS.md before this node. The reviewed decisions override provisional recommendations in audit reports. Worktree: /Users/satan/side/experiments/tracedecay/.worktrees/pluggable-memory-providers-v2. Use this exact workdir on every shell call; confirm the actual execution HEAD and peer changes before editing.

Model: gpt-5.6-luna. Reasoning: max. Spawn with fork_turns=none and a bounded handoff. You are a leaf; no subagents. You are not alone in the codebase: preserve peers' edits, never revert/reformat/stage them, and send cross-scope needs to root.

Mode: write-capable.
Prerequisites: rn-native-port. Every named predecessor must be accepted by root before dependent work starts. Original baseline: b3b43410e47115056f2066449aafa1822bbb6049; audited product head: 571daf3a9612e5247443e4da3a107b542686c1ef.

Read these reports under ../evidence/: host-delivery.md, native-facts.md, native-sessions.md, core-interface.md. Also read the exact predecessor outputs supplied by root; do not infer an unfinished API.

## Ownership and Constraints

Write only:

- crates/tracedecay/src/daemon/retained_owner/cognitive_recall.rs
- crates/tracedecay/src/mcp/server/requests/tool_dispatch.rs

Everything else is out of scope. Original Native/session/LCM implementations, original storage and contracts, NCM internals, live databases/settings, unrelated docs, manifests and generated files are protected unless explicitly named above. An allowed directory does not permit editing an original file protected by the reviewed decision. Shared generated outputs outside this ownership require an explicit root assignment before generation writes them.

No push, merge, release, global install, runtime user-data action or unassigned cleanup. Only the designated build owner in rn-build-reference and rn-build submits Cargo work. A no-change conclusion is valid when evidence proves the required behavior already holds; it must not hide missing coverage.

## Steps

1. Use the accepted contract decision to recognize the original canonical Native contribution through typed route/identity evidence. Native selection uses that original context path once, preserving its query policy and output; do not run a second Native advisory query.
2. Do not manufacture an empty provider terminal when no provider call ran. Preserve truthful capture/trace accounting and separately callable generic Native recall.
3. Remove only Native staged-reference attestation and staged-only fixtures once no Native candidate can emit those references. Preserve generic provider provenance, untrusted-text handling and NCM hydration.
4. Keep NCM's selected advisory contribution and configured observers. Canonical facts/session/LCM remain shared host authorities; do not remove them when NCM is selected.
5. Keep exact tracedecay_context admission, host lifecycle distinctions, typed failures and existing budgets. No new ordinary-prompt recall or generic duplicate-policy redesign.

Required dispatch rule: for the trusted identity/revision-bound canonical Native delivery marker, bypass the post-handler `advisory_context_call` path entirely. Test exactly one eligible canonical memory_matches execution and zero Native provider invocations, with unchanged request fields, limit, order, omissions and final Native bytes. With include_memory=false or canonical refusal/empty output, preserve the original behavior and do not add a substitute recall. Capture no fabricated provider terminal. Selected NCM/injected providers still take their real advisory path.

## Acceptance Checklist

- Native selected output matches the original canonical context contribution and contains no extra Native query/candidate copy.
- Actual invocation/capture records distinguish original canonical delivery from a provider call.
- NCM still delivers its attributed advisory contribution with shared canonical evidence and original withholding controls.

## Operator Guidance

Root launches this node from the saved execution graph only when its predecessors are accepted and its exact files are free. Review this diff before releasing dependent writers. Root alone updates plan status.

Return: node ID; exact changed paths and reasons; diff; verification commands and actual results; build tickets if applicable; protected-behavior evidence; unresolved dependencies/failures with exact next owner. Do not claim an unrun check passed.

Stop condition: Return only the host seam diff and meaningful delivery assertions. If existing typed evidence cannot express the route, send the precise type request to rn-contract via root.

