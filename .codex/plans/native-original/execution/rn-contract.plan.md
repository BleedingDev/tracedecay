---
name: rn-contract
overview: "Make the product interface fit complete original Native. Preserve the complete original upstream Native implementation from 570 and its operation boundaries."
todos:
  - id: rn-contract-done
    content: "Make the product interface fit complete original Native and return the required reviewable evidence."
    status: pending
isProject: false
---

# Make the product interface fit complete original Native

## Execution Notes

Read ../WORKER-RULES.md and ../REVIEWED-DECISIONS.md before this node. The reviewed decisions override provisional recommendations in audit reports. Worktree: /Users/satan/workspace/bleedingdev/projects/tracedecay/.worktrees/pluggable-memory-providers-v2. Use this exact workdir on every shell call; confirm the actual execution HEAD and peer changes before editing.

Execution host: Codex. Use an available Codex-native execution/review agent when assigned; adapt unavailable model preferences within Codex. You are a leaf; no child agents or cross-host agent CLI launches. Preserve peers' edits and send cross-scope needs to the lead.

Mode: write-capable.
Prerequisites: rn-source-docs, rn-integration-readiness. Every named predecessor must be accepted by root before dependent work starts. Original baseline: 57006f60cb45bcee8487e73a40d4fad1a12ee2b6; audited product head: 1fe250fed7ca615f1dfdcd580befeb276328e910.

Read these reports under ../evidence/: core-interface.md, native-facts.md, native-sessions.md. Also read the exact predecessor outputs supplied by root; do not infer an unfinished API.

## Ownership and Constraints

Write only:

- crates/tracedecay-memory-provider-api/** (excluding Cargo manifests)
- product/contracts/memory-provider-v1/**; generator outputs only inside this directory or the owned provider-API crate

Everything else is out of scope. Original Native/session/LCM implementations, original storage and contracts, NCM internals, live databases/settings, unrelated docs, manifests and generated files are protected unless explicitly named above. An allowed directory does not permit editing an original file protected by the reviewed decision. Shared generated outputs outside this ownership require an explicit root assignment before generation writes them.

No push, merge, release, global install, runtime user-data action or unassigned cleanup. Only the designated build owner in rn-build-reference and rn-build submits Cargo work. A no-change conclusion is valid when evidence proves the required behavior already holds; it must not hide missing coverage.

Required handoff: `.codex/plans/native-original/execution-results/native-operation-routes.md`. Enumerate every original fact, session, refresh/task-session and LCM public operation/background responsibility with exact 570 caller, current production-composition route, owner, read/write effect, receipt and named executable case. Unsupported generic operations must be separately justified by absence of an exact original equivalent; an original operation without a current connection blocks completion.

Known generated outputs within the owned contract directory are `product/contracts/memory-provider-v1/generated/rust/manifest.json`, `product/contracts/memory-provider-v1/generated/rust/memory_provider_v1.rs`, `product/contracts/memory-provider-v1/goldens/fixtures.jsonl` and `product/contracts/memory-provider-v1/goldens/manifest.json`. Generator/checker scripts remain read-only; do not hand-edit generated output. Confirm this inventory against the current generator before writing.

Also own `.codex/plans/native-original/execution-results/contract-outputs.md`: enumerate exact existing generator inputs/outputs before running it. Outputs outside the two owned roots require root to update ownership/dependencies first. No unbounded generated-file ownership. The once-delivered Native context marker is trusted host registration/composition metadata bound to selected identity/revision and canonical request; provider payloads/descriptors/names cannot self-assert it.

## Steps

1. Build a compact operation-to-route table from the reviewed decision: original retained fact commands, read-only automatic context, original session/LCM services and unsupported generic observations. Record concrete current public ports; keep original routes rather than adding generic equivalents for every helper.
2. Change only a demonstrated product contract gap. Keep the generic Recall effect policy read-only where it maps to original reads; explicit Search keeps the original retained command and telemetry settlement. Do not force canonical receipts into provider-local generation.
3. Define Native automatic context as the already-executed original canonical contribution, with explicit typed route identity and no second provider invocation. Reuse existing context contribution/route types when they suffice. Give host/registry owners an exact accepted decision and required type identities.
4. Keep Native outside the common advisory profile, NCM's existing common validation, and unsupported generic operations explicit. Update unreleased contracts in place and run the existing generator only for changed canonical inputs.
5. Publish exact imports, payloads, capability decisions, unchanged-contract decisions and focused fixtures for downstream owners. An unchanged API is a valid result when existing original ports suffice.

## Acceptance Checklist

- The operation table has concrete original routes for facts, sessions, LCM and background work; unsupported generic operations cannot hide a missing original route.
- Contract/serialization checks cover real changed fields and distinguish explicit Search from automatic reads; no blanket effect/version change.
- One generator remains authoritative; no original implementation or retained contract was edited.

## Operator Guidance

Root launches this node from the saved execution graph only when its predecessors are accepted and its exact files are free. Review this diff before releasing dependent writers. Root alone updates plan status.

Return: node ID; exact changed paths and reasons; diff; verification commands and actual results; build tickets if applicable; protected-behavior evidence; unresolved dependencies/failures with exact next owner. Do not claim an unrun check passed.

Stop condition: Return the bounded interface diff and complete route decision. If a required original route cannot be connected externally, report its exact signature/caller gap to root; do not patch original code.

## Current dependency contract

Prerequisites: rn-source-docs, rn-integration-readiness. The exact edges in ../execution-selection.json are authoritative; this section supersedes older prerequisite prose. Read ../READINESS-PLAN.md for the latest NCM repair and semantic scope.
