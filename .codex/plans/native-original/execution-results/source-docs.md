# rn-source-docs — root acceptance

Root accepts revised documentation/inventory authoring on 2026-09-11: 85 paths,57modified,28added,278hunks. Paths and hunk headers cross-checked. Moved source references corrected; old floor metadata unchanged. Map explicitly covers legacy13fact/memorycatalog subset; rn-contract supplies full original operation map. Direct explicit Search telemetry is distinguished from generic read-only recall. ADR scopes generic restrictions, original typed routes, and once-delivered canonical context.

Acceptance is of accurate documentation, not source parity. The shared detector_kernel.rs entropy predicate is an explicit unresolved original algorithm difference. rn-privacy-audit and its required implementation correction are mandatory before candidate acceptance/build evidence. No permission to preserve changed Native sanitization is implied. Root moved the audit dependency to rn-build so unrelated contract authoring and exact anchor restoration can proceed using this completed inventory.

Checks: inventory/path/JSON/diff checks passed; floor checks passed; corrected map checker and8existing tests passed through rn-map-checker. No Cargo.
